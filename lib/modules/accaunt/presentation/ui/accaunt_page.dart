import 'package:flutter/material.dart';

class AccauntPage extends StatelessWidget {
  const AccauntPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Accaunt Page"),
    );
  }
}